import { TClassDirective } from './t-class.directive';

describe('TClassDirective', () => {
  it('should create an instance', () => {
    const directive = new TClassDirective();
    expect(directive).toBeTruthy();
  });
});
